const express = require('express');const hbs = require('hbs');const Controlers = require('../controlers');const config = require('../config');module.exports = function (app) {    app.use(express.static('public'));    app.set('view engine', 'hbs');    app.set('views', __dirname );    app.get('/list/:period/:competitionId',function (req, res){        let partofdate = Controlers.getStrDate(  parseInt(req.params.period) )        let args = {            credentials:config.credentials,            competitionId: req.params.competitionId,            period: req.params.period,            startFrom: partofdate+'T15:20:54.120+04:00'        };        Controlers.soap_request(args, req, res, 'list')    });    app.get('/fs_list/:period/:competitionIdList',function (req, res){        let partofdate = Controlers.getStrDate(  parseInt(req.params.period) )        let valid_competitionIdList = Controlers.getValid_competitionIdList(req.params.competitionIdList);        let args = {            credentials:config.credentials,            competitionIdsJSON: valid_competitionIdList,            period: req.params.period,            startFrom: partofdate+'T15:20:54.120+04:00'        };        Controlers.soap_request_for_many(args, req, res, 'fs_list')    });    app.get('/', function (req, res) {        res.redirect('/list/3/34');    });};